<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/action_bar_background_color</item>
        <item name="colorPrimaryDark">@color/action_bar_top_color</item>
        <item name="colorAccent">@color/action_bar_text_color</item>
        <item name="toolbarStyle">@style/CustomToolBarStyle</item>
    </style>

    <!-- ActionBar is no longer Toolbar -->

    <style name="CustomToolBarStyle" parent="@style/Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@color/action_bar_text_color</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Button" parent="Base.Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/action_bar_text_color</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="AppTheme.ButtonSuccess" parent="Base.Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/colorSuccess</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
